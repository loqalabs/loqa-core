server:
  http_listen_port: 3200
  grpc_listen_port: 4317

receiver:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317

storage:
  trace:
    backend: local
    local:
      path: /tmp/tempo
